<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:gravity="center"
    android:orientation="vertical">

    <TextView
        android:id="@+id/dialog_detail_tv_shift_begin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textSize="14sp"
        tools:text="@tools:sample/date/ddmmyy" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/dialog_detail_rv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="4dp"
        android:minWidth="250dp"
        android:minHeight="250dp">

    </androidx.recyclerview.widget.RecyclerView>

    <TextView
        android:id="@+id/dialog_detail_tv_shift_end"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textSize="14sp"
        tools:text="@tools:sample/date/ddmmyy" />

    <TextView
        android:id="@+id/dialog_detail_tv_total"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:minHeight="24dp"
        android:textAlignment="center"
        android:textSize="14sp"
        tools:text="@tools:sample/date/hhmmss" />

    <Button
        android:id="@+id/dialog_detail_button_close"
        style="@style/Widget.AppCompat.Button.Borderless.Colored"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:maxHeight="48dp"
        android:minHeight="48dp"
        android:text="@string/dialog_detail_close"
        android:contentDescription="@string/dialog_detail_close_description"
        android:textSize="14sp" />

</LinearLayout>